// This is a placeholder file to replace the SendGrid version
// It doesn't actually do anything - we're using the Google Calendar URL approach instead

export default function sendCalendarInvite() {
  return { success: true };
} 